/**
 * API base URL (`.../api/v1`, no trailing slash). This is the only place to set it.
 * Example local: http://172.20.10.3:8000/api/v1
 */
export const API_BASE_URL = "https://api.habimate.com/api/v1".replace(/\/+$/, "");
